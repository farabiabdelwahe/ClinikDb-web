{
  "name": "functions",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "npx firebase emulators:start --only functions,firestore",
    "deploy": "npm run build && npx firebase deploy --only functions,firestore",
    "lint": "echo 'no lint configured'"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "private": true
}

